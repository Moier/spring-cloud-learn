server:
  port: 5555
  servlet:
    context-path: /uaa
spring:
  application:
    name: auth-server
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8083/eureka/
  instance:
    prefer-ip-address: true